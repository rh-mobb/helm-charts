{{ range .Values.dashboards }}
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ .name }}
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  json: {{ .json }}
{{ end }}

