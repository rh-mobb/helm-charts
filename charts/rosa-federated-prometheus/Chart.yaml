apiVersion: v2
name: rosa-federated-prometheus
description: A Helm chart for Kubernetes

type: application

version: 0.1.0

appVersion: "1.16.0"

# dependencies:
# - name: ocp-grafana
#   version: "0.1.0"
#   repository: "file://../ocp-grafana"