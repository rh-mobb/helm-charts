apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-me.fullname" . }}
  labels:
    {{- include "test-me.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-me.selectorLabels" . | nindent 4 }}
